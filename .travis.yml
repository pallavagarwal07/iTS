language: python
python:
    - "2.7"
notifications:
      email: false
virtualenv:
    system_site_packages: true
before_install:
 - sudo apt-get install -qq python-numpy python-scipy
install:
  - "pip install weave"
script:
        test_suite/test_all
