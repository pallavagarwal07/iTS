#include <stdio.h>
#include <stdlib.h>
#define s(n) scanf("%d",&n)
void hell(int a)
{
    printf("%d", a);
}
int main()
{
    int a =0, b=1;
    hell(123);
    if(a==0 || (b=4))
    {
        printf("%d\n", b);
    }
    return 0;
}
