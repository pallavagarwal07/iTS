long long* fmt(int *k){
    printf("Hello{World\}to you, mr sir \' \" ");
    return func(-132+'\'');
}
int main()
{
    if(k==4)
        for(i=0;i<10;)
            while(!helpArrives)
                scream();
    else
    {
        do
            printf("This will cause definite trouble\")");
        while(hello!=goodBye);
        for(i:arr)
            printf("Support this format even though im not sure if c supports it");
        printf("Here;are;some:colons;and semicolons';';';'''''''");
    }
    return 0 - enemyPrize;
}
